- link:
    ~/.gitconfig:
      path: git/gitconfig
    ~/.vimrc:
      path: vim/vimrc
    ~/.zshrc:
      path: zsh/zshrc
    ~/.config/nvim:
      path: nvim

- clean:
    ~/:
      force: true
    ~/.config:
      force: true
